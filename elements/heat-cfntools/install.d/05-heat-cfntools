#!/bin/sh

set -uex

install-packages \
    python-pip python-psutil

# a new boto is needed for this bug fix: http://pad.lv/1177576
pip install -U boto heat-cfntools
cfn-create-aws-symlinks --source /usr/local/bin
